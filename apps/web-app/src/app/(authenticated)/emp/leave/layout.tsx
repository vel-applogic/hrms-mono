import { EmpLeaveView } from '@/feature/emp/emp-leave-view';

export default function EmpLeaveLayout({ children }: { children: React.ReactNode }) {
  return <EmpLeaveView>{children}</EmpLeaveView>;
}
