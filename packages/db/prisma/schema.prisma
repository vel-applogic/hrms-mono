generator client {
  provider = "prisma-client"
  output   = "../generated/client"
}

datasource db {
  provider = "postgresql"
}

// MEDIA
model Media {
  id             Int             @id @default(autoincrement())
  organisationId Int             @map("organisation_id")
  organisation   Organisation    @relation(fields: [organisationId], references: [id])
  key            String
  thumbnailKey   String?         @map("thumbnail_key")
  type           MediaTypeDbEnum
  name           String
  size           Int
  ext            String
  createdAt      DateTime        @default(now()) @map("created_at")
  updatedAt      DateTime        @updatedAt @map("updated_at")

  candidateHasMedias          CandidateHasMedia[]
  policyHasMedias             PolicyHasMedia[]
  employeeHasMedias           EmployeeHasMedia[]
  leaveHasMedias              LeaveHasMedia[]
  organisationLogos           Organisation[]               @relation("OrganisationLogo")
  organisationHasDocuments    OrganisationHasDocument[]
  employyBgvFeedbackHasMedias employyBgvFeedbackHasMedia[]
  deviceHasMedias             DeviceHasMedia[]
  reimbursementHasMedias      ReimbursementHasMedia[]

  @@map("media")
}

// CURRENCY
model Currency {
  id            Int            @id @default(autoincrement())
  code          String         @unique
  name          String
  symbol        String?
  organisations Organisation[]

  @@map("currency")
}

// COUNTRY

model Country {
  id        Int       @id @default(autoincrement())
  name      String
  code      String    @unique
  addresses Address[]

  @@map("country")
}

// ADDRESS
model Address {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  countryId      Int          @map("country_id")
  country        Country      @relation(fields: [countryId], references: [id])
  addressLine1   String       @map("address_line_1")
  addressLine2   String       @map("address_line_2")
  city           String       @map("city")
  state          String       @map("state")
  postalCode     String       @map("postal_code")
  latitude       Float?       @map("latitude")
  longitude      Float?       @map("longitude")

  createdAt                DateTime                 @default(now()) @map("created_at")
  updatedAt                DateTime                 @updatedAt @map("updated_at")
  organisationHasAddresses OrganisationHasAddress[]

  @@map("address")
}

// CONTACT

enum ContactType {
  phone
  email
  website
  socialMediaLink
}

model Contact {
  id                      Int                      @id @default(autoincrement())
  organisationId          Int                      @map("organisation_id")
  organisation            Organisation             @relation(fields: [organisationId], references: [id])
  contact                 String                   @map("contact_number")
  contactType             ContactType              @map("contact_type")
  createdAt               DateTime                 @default(now()) @map("created_at")
  updatedAt               DateTime                 @updatedAt @map("updated_at")
  organisationHasContacts OrganisationHasContact[]

  @@map("contact")
}

// USER

model User {
  id             Int          @id @default(autoincrement())
  email          String       @unique
  firstname      String
  lastname       String
  password       String
  gender         GenderDbEnum
  isSuperAdmin   Boolean      @default(false) @map("is_super_admin")
  isActive       Boolean      @default(true) @map("is_active")
  lastLoggedInAt DateTime?    @map("last_logged_in_at")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  auditActivities                 AuditActivity[]
  userForgotPasswords             UserForgotPassword[]
  userVerifyEmails                UserVerifyEmail[]
  userInvites                     UserInvite[]               @relation("UserInviteUser")
  sentInvites                     UserInvite[]               @relation("UserInviteInvitedBy")
  candidateHasFeedbacks           CandidateHasFeedback[]
  reportTo                        Employee[]                 @relation("EmployeeReportTo")
  employees                       Employee[]                 @relation("EmployeeUser")
  employeeHasMedias               EmployeeHasMedia[]         @relation("EmployeeHasMediaUser")
  payrollCompensations            PayrollCompensation[]      @relation("EmployeeCompensationUser")
  employeeFeedbacksCreatedBy      EmployeeFeedback[]         @relation("EmployeeFeedbackCreatedBy")
  employeeFeedbacks               EmployeeFeedback[]         @relation("EmployeeFeedbackUser")
  leaves                          Leave[]
  employeeLeaveCounters           EmployeeLeaveCounter[]
  payrollDeductions               PayrollDeduction[]
  payrollPayslips                 PayrollPayslip[]
  organisationHasUsers            OrganisationHasUser[]
  userInBranches                  UserInBranch[]
  employeeBgvFeedbacks            employeeBgvFeedback[]      @relation("EmployeeBgvFeedbackUser")
  assignedDevices                 Device[]                   @relation("DeviceAssignedTo")
  devicePossessionHistories       DevicePossessionHistory[]
  reimbursements                  Reimbursement[]
  reimbursementFeedbacksCreatedBy ReimbursementHasFeedback[]
  userInDepartments               UserInDepartment[]
  notifications                   Notification[]

  @@map("users")
}

model UserForgotPassword {
  id                         Int       @id @default(autoincrement())
  userId                     Int       @map("user_id")
  user                       User      @relation(fields: [userId], references: [id])
  forgotPasswordKey          String?   @map("forgot_password_key")
  forgotPasswordKeyCreatedAt DateTime  @default(now()) @map("forgot_password_key_created_at")
  resetAt                    DateTime? @map("reset_at")

  @@map("user_forgot_password")
}

model UserVerifyEmail {
  id                      Int       @id @default(autoincrement())
  userId                  Int       @map("user_id")
  user                    User      @relation(fields: [userId], references: [id])
  verifyEmailKey          String?   @map("verify_email_key")
  verifyEmailKeyCreatedAt DateTime  @default(now()) @map("verify_email_key_created_at")
  verifiedAt              DateTime? @map("verified_at")

  @@map("user_verify_email")
}

model UserInBranch {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation(fields: [userId], references: [id])
  branchId       Int          @map("branch_id")
  branch         Branch       @relation(fields: [branchId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([userId, branchId])
}

model UserInvite {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation("UserInviteUser", fields: [userId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  inviteKey      String       @map("invite_key")
  invitedById    Int          @map("invited_by_id")
  invitedBy      User         @relation("UserInviteInvitedBy", fields: [invitedById], references: [id])
  acceptedAt     DateTime?    @map("accepted_at")
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  @@index([userId])
  @@index([organisationId])
  @@map("user_invite")
}

model DevicePossessionHistory {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  userId         Int          @map("user_id")
  user           User         @relation(fields: [userId], references: [id])
  deviceId       Int          @map("device_id")
  device         Device       @relation(fields: [deviceId], references: [id])
  fromDate       DateTime     @map("from_date")
  toDate         DateTime?    @map("to_date")
  notes          String[]     @map("notes")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([userId])
  @@index([deviceId])
  @@index([organisationId])
  @@map("device_possession_history")
}

// ORGANISATION
model Organisation {
  id         Int      @id @default(autoincrement())
  name       String
  logoId     Int?     @map("logo_id")
  logo       Media?   @relation("OrganisationLogo", fields: [logoId], references: [id])
  website    String?  @map("website")
  currencyId Int      @map("currency_id")
  currency   Currency @relation(fields: [currencyId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  organisationHasUsers      OrganisationHasUser[]
  branches                  Branch[]
  userInBranches            UserInBranch[]
  userInvites               UserInvite[]
  employees                 Employee[]
  payrollCompensations      PayrollCompensation[]
  payrollDeductions         PayrollDeduction[]
  payrollPayslips           PayrollPayslip[]
  employeeFeedbacks         EmployeeFeedback[]
  employeeLeaveCounters     EmployeeLeaveCounter[]
  leaves                    Leave[]
  candidates                Candidate[]
  policies                  Policy[]
  medias                    Media[]
  settings                  OrganisationSetting[]
  organisationHasDocuments  OrganisationHasDocument[]
  employeeBgvFeedbacks      employeeBgvFeedback[]
  addresses                 Address[]
  contacts                  Contact[]
  organisationHasContacts   OrganisationHasContact[]
  organisationHasAddresses  OrganisationHasAddress[]
  holidays                  Holiday[]
  devices                   Device[]
  devicePossessionHistories DevicePossessionHistory[]
  expenses                  Expense[]
  expenseForecasts          ExpenseForecast[]
  reimbursements            Reimbursement[]
  departments               Department[]
  userInDepartments         UserInDepartment[]
  notifications             Notification[]
  announcements             Announcement[]

  @@map("organisation")
}

model OrganisationHasContact {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  contactId      Int          @map("contact_id")
  contact        Contact      @relation(fields: [contactId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([organisationId, contactId])
  @@map("organisation_has_contact")
}

model OrganisationHasAddress {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  addressId      Int          @map("address_id")
  address        Address      @relation(fields: [addressId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([organisationId, addressId])
  @@map("organisation_has_address")
}

model OrganisationHasUser {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  userId         Int          @map("user_id")
  user           User         @relation(fields: [userId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  roles UserRoleDbEnum[]

  @@unique([organisationId, userId])
  @@map("organisation_has_user")
}

enum NoOfDaysInMonthDbEnum {
  dynamic
  thirty
  thirtyOne
}

model OrganisationSetting {
  id                    Int                   @id @default(autoincrement())
  organisationId        Int                   @map("organisation_id")
  organisation          Organisation          @relation(fields: [organisationId], references: [id])
  noOfDaysInMonth       NoOfDaysInMonthDbEnum @default(thirty) @map("no_of_days_in_month")
  totalLeaveInDays      Int                   @default(24) @map("total_leave_in_days")
  sickLeaveInDays       Int                   @default(10) @map("sick_leave_in_days")
  earnedLeaveInDays     Int                   @default(10) @map("earned_leave_in_days")
  casualLeaveInDays     Int                   @default(10) @map("casual_leave_in_days")
  maternityLeaveInDays  Int                   @default(10) @map("maternity_leave_in_days")
  paternityLeaveInDays  Int                   @default(10) @map("paternity_leave_in_days")
  weeklyOffDays         Int[]                 @default([0, 6]) @map("weekly_off_days")
  financialYearStartsAt Int                   @default(4) @map("financial_year_starts_at")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("organisation_settings")
}

model OrganisationHasDocument {
  id             Int             @id @default(autoincrement())
  organisationId Int             @map("organisation_id")
  organisation   Organisation    @relation(fields: [organisationId], references: [id])
  documentId     Int             @map("document_id")
  document       Media           @relation(fields: [documentId], references: [id])
  mediaType      MediaTypeDbEnum @map("media_type")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("organisation_has_document")
}

// BRANCH
model Branch {
  id             Int          @id @default(autoincrement())
  name           String
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  userInBranches UserInBranch[]
  employees      Employee[]
  announcements  Announcement[]

  @@map("branche")
}

// DEPARTMENT
model Department {
  id             Int          @id @default(autoincrement())
  name           String
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  userInDepartments UserInDepartment[]
  employees         Employee[]
  announcements     Announcement[]

  @@unique([name, organisationId])
  @@map("department")
}

model UserInDepartment {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation(fields: [userId], references: [id])
  departmentId   Int          @map("department_id")
  department     Department   @relation(fields: [departmentId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([userId, departmentId])
  @@map("user_in_department")
}

// EMPLOYEE

model Employee {
  id                           Int                @id @default(autoincrement())
  userId                       Int                @map("user_id")
  user                         User               @relation("EmployeeUser", fields: [userId], references: [id])
  organisationId               Int                @map("organisation_id")
  organisation                 Organisation       @relation(fields: [organisationId], references: [id])
  employeeCode                 String             @map("employee_code")
  personalEmail                String?            @map("personal_email")
  dob                          DateTime
  pan                          String?
  aadhaar                      String?
  designation                  String
  dateOfJoining                DateTime           @map("date_of_joining")
  dateOfLeaving                DateTime?          @map("date_of_leaving")
  status                       EmployeeStatusEnum @map("status")
  reportToId                   Int?               @map("report_to_id")
  reportTo                     User?              @relation("EmployeeReportTo", fields: [reportToId], references: [id])
  isBgVerified                 Boolean            @default(false) @map("is_bg_verified")
  emergencyContactName         String             @map("emergency_contact_name")
  emergencyContactNumber       String             @map("emergency_contact_number")
  emergencyContactRelationship String             @map("emergency_contact_relationship")
  branchId                     Int?               @map("branch_id")
  branch                       Branch?            @relation(fields: [branchId], references: [id])
  departmentId                 Int?               @map("department_id")
  department                   Department?        @relation(fields: [departmentId], references: [id])
  candidateId                  Int?               @map("candidate_id")
  candidate                    Candidate?         @relation("EmployeeCandidate", fields: [candidateId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  candidatesConverted Candidate[] @relation("CandidateEmployee")

  @@unique([userId, organisationId])
  @@unique([employeeCode, organisationId])
  @@unique([pan, organisationId])
  @@unique([aadhaar, organisationId])
  @@index([userId])
  @@index([reportToId])
  @@index([status])
  @@map("user_employee_detail")
}

model employeeBgvFeedback {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation("EmployeeBgvFeedbackUser", fields: [userId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  feedback       String       @map("feedback")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  employyBgvFeedbackHasMedias employyBgvFeedbackHasMedia[]

  @@index([userId])
  @@map("employee_bgv_feedback")
}

model employyBgvFeedbackHasMedia {
  id                    Int                 @id @default(autoincrement())
  employeeBgvFeedbackId Int                 @map("employee_bgv_feedback_id")
  employeeBgvFeedback   employeeBgvFeedback @relation(fields: [employeeBgvFeedbackId], references: [id])
  mediaId               Int                 @map("media_id")
  media                 Media               @relation(fields: [mediaId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([employeeBgvFeedbackId])
  @@index([mediaId])
  @@map("employee_bgv_feedback_has_media")
}

model EmployeeHasMedia {
  id      Int               @id @default(autoincrement())
  userId  Int               @map("user_id")
  user    User              @relation("EmployeeHasMediaUser", fields: [userId], references: [id])
  mediaId Int               @map("media_id")
  media   Media             @relation(fields: [mediaId], references: [id])
  type    EmployeeMediaType
  caption String?           @map("caption")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([userId])
  @@map("employee_has_media")
}

model PayrollCompensation {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation("EmployeeCompensationUser", fields: [userId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  grossAmount    Float        @map("gross_amount")
  effectiveFrom  DateTime     @map("effective_from") @db.Date
  effectiveTill  DateTime?    @map("effective_till") @db.Date
  isActive       Boolean      @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  payrollCompensationLineItems PayrollCompensationLineItem[]

  @@index([userId])
  @@map("employee_compensation")
}

model PayrollCompensationLineItem {
  id                    Int                 @id @default(autoincrement())
  payrollCompensationId Int                 @map("payroll_compensation_id")
  payrollCompensation   PayrollCompensation @relation(fields: [payrollCompensationId], references: [id])
  title                 String              @map("title")
  amount                Float               @map("amount")
  createdAt             DateTime            @default(now()) @map("created_at")
  updatedAt             DateTime            @updatedAt @map("updated_at")

  @@map("payroll_compensation_line_item")
}

model PayrollDeduction {
  id             Int          @id @default(autoincrement())
  userId         Int          @map("user_id")
  user           User         @relation(fields: [userId], references: [id])
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  effectiveFrom  DateTime     @map("effective_from") @db.Date
  effectiveTill  DateTime?    @map("effective_till") @db.Date
  isActive       Boolean      @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  payrollDeductionLineItems PayrollDeductionLineItem[]

  @@index([userId])
  @@map("user_employee_deduction")
}

model PayrollDeductionLineItem {
  id                 Int                       @id @default(autoincrement())
  payrollDeductionId Int                       @map("payroll_deduction_id")
  payrollDeduction   PayrollDeduction          @relation(fields: [payrollDeductionId], references: [id])
  type               PayrollDeductionType      @map("type")
  frequency          PayrollDeductionFrequency @map("frequency")
  amount             Float                     @map("amount")
  otherTitle         String?                   @map("other_title")
  specificMonth      DateTime?                 @map("specific_month") @db.Date

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([payrollDeductionId, type, frequency])
  @@map("payroll_deduction_line_item")
}

model EmployeeFeedback {
  id             Int                   @id @default(autoincrement())
  userId         Int                   @map("user_id")
  user           User                  @relation("EmployeeFeedbackUser", fields: [userId], references: [id])
  organisationId Int                   @map("organisation_id")
  organisation   Organisation          @relation(fields: [organisationId], references: [id])
  trend          EmployeeFeedbackTrend @map("trend")
  point          Int?                  @default(0) @map("point")
  title          String                @map("title")
  feedback       String                @map("feedback")
  createdById    Int                   @map("created_by_id")
  createdBy      User                  @relation("EmployeeFeedbackCreatedBy", fields: [createdById], references: [id])
  createdAt      DateTime              @default(now()) @map("created_at")
  updatedAt      DateTime              @updatedAt @map("updated_at")

  @@index([userId])
  @@map("user_employee_feedback")
}

model EmployeeLeaveCounter {
  id                   Int          @id @default(autoincrement())
  userId               Int          @map("user_id")
  user                 User         @relation(fields: [userId], references: [id])
  organisationId       Int          @map("organisation_id")
  organisation         Organisation @relation(fields: [organisationId], references: [id])
  casualLeaves         Float        @map("casual_leaves")
  sickLeaves           Float        @map("sick_leaves")
  earnedLeaves         Float        @map("earned_leaves")
  totalLeavesUsed      Float        @map("total_leaves_used")
  totalLeavesAvailable Float        @map("total_leaves_available")
  financialYear        String       @map("financial_year")
  createdAt            DateTime     @default(now()) @map("created_at")
  updatedAt            DateTime     @updatedAt @map("updated_at")

  @@unique([userId, organisationId, financialYear])
  @@index([userId, organisationId])
  @@map("user_employee_leave_counter")
}

// PAYROLL
model PayrollPayslip {
  id                      Int                      @id @default(autoincrement())
  userId                  Int                      @map("user_id")
  user                    User                     @relation(fields: [userId], references: [id])
  organisationId          Int                      @map("organisation_id")
  organisation            Organisation             @relation(fields: [organisationId], references: [id])
  month                   Int                      @map("month")
  year                    Int                      @map("year")
  createdAt               DateTime                 @default(now()) @map("created_at")
  updatedAt               DateTime                 @updatedAt @map("updated_at")
  grossAmount             Int                      @map("gross_amount")
  netAmount               Int                      @map("net_amount")
  deductionAmount         Int                      @map("deduction_amount")
  pdfS3Key                String                   @map("pdf_s3_key")
  payrollPayslipLineItems PayrollPayslipLineItem[]

  @@unique([userId, month, year])
  @@index([userId])
  @@map("payslip")
}

model PayrollPayslipLineItem {
  id          Int                        @id @default(autoincrement())
  payslipId   Int                        @map("payslip_id")
  payslip     PayrollPayslip             @relation(fields: [payslipId], references: [id])
  type        PayrollPayslipLineItemType @map("type")
  amount      Int                        @map("amount")
  title       String                     @map("title")
  description String?                    @map("description")
  createdAt   DateTime                   @default(now()) @map("created_at")
  updatedAt   DateTime                   @updatedAt @map("updated_at")

  @@index([payslipId])
  @@map("payslip_line_item")
}

// LEAVES
model Leave {
  id              Int              @id @default(autoincrement())
  userId          Int              @map("user_id")
  user            User             @relation(fields: [userId], references: [id])
  organisationId  Int              @map("organisation_id")
  organisation    Organisation     @relation(fields: [organisationId], references: [id])
  leaveType       LeaveTypeEnum    @map("leave_type")
  startDate       DateTime         @map("start_date") @db.Date
  endDate         DateTime         @map("end_date") @db.Date
  startDuration   LeaveDayHalfEnum @default(full) @map("start_duration")
  endDuration     LeaveDayHalfEnum @default(full) @map("end_duration")
  numberOfDays    Float            @map("number_of_days")
  numberOfLopDays Float            @default(0) @map("number_of_lop_days")
  isConsumed      Boolean          @default(false) @map("is_consumed")
  reason          String           @map("reason")
  status          LeaveStatusEnum  @default(pending) @map("status")
  createdAt       DateTime         @default(now()) @map("created_at")
  updatedAt       DateTime         @updatedAt @map("updated_at")
  leaveHasMedias  LeaveHasMedia[]

  @@index([userId])
  @@map("leave")
}

model LeaveHasMedia {
  id        Int      @id @default(autoincrement())
  leaveId   Int      @map("leave_id")
  leave     Leave    @relation(fields: [leaveId], references: [id])
  mediaId   Int      @map("media_id")
  media     Media    @relation(fields: [mediaId], references: [id])
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([leaveId])
  @@index([mediaId])
  @@map("leave_has_media")
}

// CANDIDATE

model CandidateHasMedia {
  id          Int                @id @default(autoincrement())
  candidateId Int                @map("candidate_id")
  mediaId     Int                @map("media_id")
  createdAt   DateTime           @default(now()) @map("created_at")
  updatedAt   DateTime           @updatedAt @map("updated_at")
  type        CandidateMediaType @map("type")
  media       Media              @relation(fields: [mediaId], references: [id])
  candidate   Candidate          @relation(fields: [candidateId], references: [id])
}

model Candidate {
  id                 Int               @id @default(autoincrement())
  organisationId     Int               @map("organisation_id")
  organisation       Organisation      @relation(fields: [organisationId], references: [id])
  firstname          String
  lastname           String
  email              String
  gender             GenderDbEnum
  contactNumbers     String[]
  source             CandidateSource
  urls               String[]
  expInYears         Float
  relevantExpInYears Float
  skills             String[]
  currentCtcInLacs   Float
  expectedCtcInLacs  Float
  noticePeriod       Float
  noticePeriodUnit   NoticePeriodUnit
  status             CandidateStatus   @default(new)
  progress           CandidateProgress @default(new)
  dob                DateTime?
  pan                String?
  aadhaar            String?
  employeeId         Int?              @map("employee_id")
  employee           Employee?         @relation("CandidateEmployee", fields: [employeeId], references: [id])
  employeeSource     Employee[]        @relation("EmployeeCandidate")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  isDeleted             Boolean                @default(false) @map("is_deleted")
  candidateHasMedias    CandidateHasMedia[]
  candidateHasFeedbacks CandidateHasFeedback[]

  @@unique([email, organisationId])
  @@index([organisationId])
  @@map("candidate")
}

model CandidateHasFeedback {
  id          Int       @id @default(autoincrement())
  candidateId Int       @map("candidate_id")
  createdById Int       @map("created_by_id")
  feedback    String
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
  candidate   Candidate @relation(fields: [candidateId], references: [id])
  createdBy   User      @relation(fields: [createdById], references: [id])

  @@index([candidateId])
  @@map("candidate_has_feedback")
}

// POLICIES
model Policy {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  title          String
  content        Json

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  policyHasMedias PolicyHasMedia[]

  @@map("policy")
}

model PolicyHasMedia {
  id       Int    @id @default(autoincrement())
  policyId Int    @map("policy_id")
  policy   Policy @relation(fields: [policyId], references: [id])
  mediaId  Int    @map("media_id")
  media    Media  @relation(fields: [mediaId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("policy_has_media")
}

// ANNOUNCEMENT
model Announcement {
  id                 Int          @id @default(autoincrement())
  organisationId     Int          @map("organisation_id")
  organisation       Organisation @relation(fields: [organisationId], references: [id])
  title              String
  message            String
  branchId           Int?         @map("branch_id")
  branch             Branch?      @relation(fields: [branchId], references: [id])
  departmentId       Int?         @map("department_id")
  department         Department?  @relation(fields: [departmentId], references: [id])
  scheduledAt        DateTime     @map("scheduled_at")
  isPublished        Boolean      @default(false) @map("is_published")
  isNotificationSent Boolean      @default(false) @map("is_notification_sent")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([isPublished, isNotificationSent, scheduledAt])
  @@map("announcement")
}

// HOLIDAY LIST

enum HolidayType {
  national
  state

  @@map("holidayType")
}

model Holiday {
  id             Int           @id @default(autoincrement())
  organisationId Int           @map("organisation_id")
  organisation   Organisation  @relation(fields: [organisationId], references: [id])
  name           String
  date           DateTime      @map("date") @db.Date
  types          HolidayType[] @map("types")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("holiday")
}

// DEVICE

enum DeviceType {
  mobile
  tablet
  laptop
  cpu
  keyboard
  mouse
  headphone
  other
}

enum DeviceStatus {
  good
  physicallyDamaged
  notWorking
  lost
  stolen
}

model Device {
  id                  Int                       @id @default(autoincrement())
  organisationId      Int                       @map("organisation_id")
  organisation        Organisation              @relation(fields: [organisationId], references: [id])
  type                DeviceType                @map("type")
  brand               String                    @map("brand")
  model               String                    @map("model")
  serialNumber        String                    @map("serial_number")
  price               Float                     @map("price")
  purchasedAt         DateTime?                 @map("purchased_at") @db.Date
  warrantyExpiresAt   DateTime                  @map("warranty_expires_at") @db.Date
  inWarranty          Boolean                   @default(true) @map("in_warranty")
  status              DeviceStatus              @map("status")
  config              String?                   @map("config")
  assignedToId        Int?                      @map("assigned_to_id")
  assignedTo          User?                     @relation("DeviceAssignedTo", fields: [assignedToId], references: [id])
  createdAt           DateTime                  @default(now()) @map("created_at")
  updatedAt           DateTime                  @updatedAt @map("updated_at")
  deviceHasMedias     DeviceHasMedia[]
  possessionHistories DevicePossessionHistory[]

  @@map("device")
}

model DeviceHasMedia {
  id       Int     @id @default(autoincrement())
  deviceId Int     @map("device_id")
  device   Device  @relation(fields: [deviceId], references: [id])
  mediaId  Int     @map("media_id")
  media    Media   @relation(fields: [mediaId], references: [id])
  caption  String? @map("caption")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("device_has_media")
}

// AUDIT

enum AuditEntityTypeDbEnum {
  user
  user_admin
  candidate
  employee
  policy
  device
  announcement

  @@map("auditEntityTypeDbEnum")
}

model AuditActivity {
  id          Int                       @id @default(autoincrement())
  eventGroup  AuditEventGroupDbEnum     @map("event_group")
  eventType   AuditEventTypeDbEnum      @map("event_type")
  status      AuditActivityStatusDbEnum
  actorId     Int?                      @map("actor_id")
  description String?
  data        Json?
  createdAt   DateTime                  @default(now()) @map("created_at")
  actor       User?                     @relation(fields: [actorId], references: [id])
  entities    AuditActivityHasEntity[]

  @@index([eventGroup, eventType])
  @@index([actorId])
  @@index([createdAt])
  @@index([status])
  @@map("audit_activity")
}

model AuditActivityHasEntity {
  id             Int                   @id @default(autoincrement())
  auditId        Int                   @map("audit_id")
  entityType     AuditEntityTypeDbEnum @map("entity_type")
  entityId       Int                   @map("entity_id")
  message        String?
  isSourceEntity Boolean               @default(false) @map("is_source_entity")
  createdAt      DateTime              @default(now()) @map("created_at")
  audit          AuditActivity         @relation(fields: [auditId], references: [id])

  @@index([auditId])
  @@index([entityType, entityId])
  @@map("audit_activity_has_entity")
}

// APP MIGRATIONS

model AppMigration {
  id        Int      @id @default(autoincrement())
  key       String   @unique
  createdAt DateTime @default(now())

  @@map("app_migrations")
}

// EXPENSE
model Expense {
  id             Int          @id @default(autoincrement())
  organisationId Int          @map("organisation_id")
  organisation   Organisation @relation(fields: [organisationId], references: [id])
  date           DateTime     @map("date") @db.Date
  description    String?      @map("description")
  type           ExpenseType  @map("type")
  amount         Float        @map("amount")
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  @@index([organisationId])
  @@map("expense")
}

model ExpenseForecast {
  id             Int                      @id @default(autoincrement())
  organisationId Int                      @map("organisation_id")
  organisation   Organisation             @relation(fields: [organisationId], references: [id])
  description    String?                  @map("description")
  type           ExpenseType              @map("type")
  amount         Float                    @map("amount")
  frequency      ExpenseForecastFrequency @map("frequency")
  createdAt      DateTime                 @default(now()) @map("created_at")
  updatedAt      DateTime                 @updatedAt @map("updated_at")

  @@index([organisationId])
  @@map("expense_forecast")
}

// REIMBURSEMENT

enum ReimbursementStatusDbEnum {
  pending
  approved
  paid
  rejected

  @@map("reimbursementStatusDbEnum")
}

model Reimbursement {
  id             Int                       @id @default(autoincrement())
  organisationId Int                       @map("organisation_id")
  organisation   Organisation              @relation(fields: [organisationId], references: [id])
  userId         Int                       @map("user_id")
  user           User                      @relation(fields: [userId], references: [id])
  title          String
  amount         Float
  date           DateTime                  @db.Date
  status         ReimbursementStatusDbEnum @default(pending)
  rejectReason   String?                   @map("reject_reason")
  approvedAt     DateTime?                 @map("approved_at")
  paidAt         DateTime?                 @map("paid_at")
  createdAt      DateTime                  @default(now()) @map("created_at")
  updatedAt      DateTime                  @updatedAt @map("updated_at")

  reimbursementHasMedias    ReimbursementHasMedia[]
  reimbursementHasFeedbacks ReimbursementHasFeedback[]

  @@index([organisationId])
  @@index([userId])
  @@map("reimbursement")
}

model ReimbursementHasMedia {
  id              Int           @id @default(autoincrement())
  reimbursementId Int           @map("reimbursement_id")
  reimbursement   Reimbursement @relation(fields: [reimbursementId], references: [id])
  mediaId         Int           @map("media_id")
  media           Media         @relation(fields: [mediaId], references: [id])
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")

  @@index([reimbursementId])
  @@index([mediaId])
  @@map("reimbursement_has_media")
}

model ReimbursementHasFeedback {
  id              Int           @id @default(autoincrement())
  reimbursementId Int           @map("reimbursement_id")
  reimbursement   Reimbursement @relation(fields: [reimbursementId], references: [id])
  createdById     Int           @map("created_by_id")
  createdBy       User          @relation(fields: [createdById], references: [id])
  message         String
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")

  @@index([reimbursementId])
  @@map("reimbursement_has_feedback")
}

// NOTIFICATION

enum NotificationLinkDbEnum {
  dashboard
  employee
  leaves
  reimbursement
  device
  payroll
  candidate
  policy
  expense
  organisation
  user
  empDashboard
  empLeave
  empDetails
  empDocuments
  empPayroll
  empDevice
  empReimbursement
  empFeedbacks
  empPolicy
  announcement
  empAnnouncement

  @@map("notificationLinkDbEnum")
}

model Notification {
  id             Int                    @id @default(autoincrement())
  organisationId Int                    @map("organisation_id")
  organisation   Organisation           @relation(fields: [organisationId], references: [id])
  userId         Int                    @map("user_id")
  user           User                   @relation(fields: [userId], references: [id])
  title          String
  message        String
  link           NotificationLinkDbEnum
  isSeen         Boolean                @default(false) @map("is_seen")
  createdAt      DateTime               @default(now()) @map("created_at")
  updatedAt      DateTime               @updatedAt @map("updated_at")

  @@index([userId, isSeen])
  @@index([organisationId])
  @@map("notification")
}

// ENUMS

enum ExpenseForecastFrequency {
  monthly
  yearly

  @@map("expenseForecastFrequency")
}

enum ExpenseType {
  incomeTax
  rent
  ai
  emailService
  server
  internet
  phone
  account
  auditor
  roc
  digitalSignature
  healthInsurance
  accidentInsurance
  travelInsurance
  liabilityInsurance
  propertyInsurance
  other

  @@map("expenseType")
}

enum UserRoleDbEnum {
  admin
  employee

  @@map("userRoleDbEnum")
}

enum GenderDbEnum {
  male
  female
  other

  @@map("genderDbEnum")
}

enum EmployeeStatusEnum {
  active
  resigned
  onLeave
  terminated

  @@map("employeeStatusEnum")
}

enum EmployeeMediaType {
  photo
  resume
  offerLetter
  otherDocuments

  @@map("employeeMediaType")
}

enum PayrollDeductionType {
  providentFund
  incomeTax
  insurance
  professionalTax
  loan
  lop
  other

  @@map("userEmployeeDeductionType")
}

enum PayrollDeductionFrequency {
  monthly
  yearly
  specificMonth

  @@map("userEmployeeDeductionFrequency")
}

enum PayrollPayslipLineItemType {
  earning
  deduction
}

enum EmployeeFeedbackTrend {
  positive
  negative
  neutral

  @@map("employeeFeedbackTrend")
}

enum LeaveTypeEnum {
  casual
  sick
  medical
  earned

  @@map("leaveTypeEnum")
}

enum LeaveStatusEnum {
  pending
  approved
  rejected
  cancelled

  @@map("leaveStatusEnum")
}

enum LeaveDayHalfEnum {
  full
  firstHalf
  secondHalf

  @@map("leaveDayHalfEnum")
}

enum MediaTypeDbEnum {
  doc
  image
  zip
  video

  @@map("mediaTypeDbEnum")
}

enum AuditEventGroupDbEnum {
  authentication
  operation

  @@map("auditEventGroupDbEnum")
}

enum AuditEventTypeDbEnum {
  login_success
  login_failure
  create
  update
  delete
  password_reset
  password_reset_request
  otp_request
  register
  email_verify
  account_activate
  confirm
  block_user
  unblock_user

  @@map("auditEventTypeDbEnum")
}

enum AuditActivityStatusDbEnum {
  success
  failure

  @@map("auditActivityStatusDbEnum")
}

enum CandidateSource {
  email
  googleSearch
  lead
  linkedin
  referral
  websiteForm

  @@map("candidateSource")
}

enum NoticePeriodUnit {
  days
  weeks
  months

  @@map("noticePeriodUnit")
}

enum CandidateStatus {
  new
  planed
  notReachable
  selected
  onHold
  rejected
}

enum CandidateProgress {
  new
  infoCollected
  lev1InterviewScheduled
  lev1InterviewCompleted
  lev2InterviewScheduled
  lev2InterviewCompleted
  offerReleased
  offerAccepted
}

enum CandidateMediaType {
  resume
  offerLetter
  otherDocuments

  @@map("candidateMediaType")
}
