# Use local pnpm store in project directory
store-dir=.pnpm-store

# Optional: Symlink node_modules structure (default is 'auto')
# node-linker=hoisted

# Optional: Shamefully hoist (if needed for compatibility)
# shamefully-hoist=true
